# STRICT RULE: NEVER USE GOOGLE GEMINI / AI STAR ICONS

## Rule:
- NEVER use, generate, or insert the Gemini / Google Bard 4-pointed AI star icon (`Sparkles` from `lucide-react` or similar AI stars/sparkles).
- The user has explicitly banned this icon from the entire project.
- ALWAYS use romantic, brand-aligned, or purpose-driven icons instead:
  - For OurStory branding, user portal, love, emotions, proposals: Use `<Heart />` or `💖` / `❤️`.
  - For celebration / gifts: Use `<Gift />` or `<PartyPopper />`.
  - For special offers / upgrades: Use `<Zap />`, `<Tag />`, or `<Flame />`.
  - For achievements / ratings: Use `<Star />` or `<Award />`.
